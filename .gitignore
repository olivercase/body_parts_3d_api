# Raw download artifacts — reproducible, not committed
chunks/
*.zip
_session_cookies.txt
download.log
_quarantine_*/

# Note: meshes/*.obj ARE committed, via Git LFS (see .gitattributes)

# Python
__pycache__/
*.pyc
.venv/
venv/

# Subsets produced by bp3d_subset.py
subset/
